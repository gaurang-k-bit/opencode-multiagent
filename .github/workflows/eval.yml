name: Multi-Agent Evaluation

on:
  push:
    branches:
      - main
  workflow_dispatch:
    inputs:
      recipient_email:
        description: "Email address to send results to"
        required: true
        default: "gskakodkar07@gmail.com"

jobs:
  evaluate:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install uv
        uses: astral-sh/setup-uv@v8.0.0

      - name: Install dependencies
        run: uv sync

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install OpenCode
        run: npm install -g opencode-ai

      - name: Authenticate OpenCode
        run: echo "OPENCODE_ZEN_API_KEY=${{ secrets.OPENCODE_ZEN_API_KEY }}" >> $GITHUB_ENV

      - name: Run evaluation
        run: uv run python evaluate.py

      - name: Upload results as artifact
        uses: actions/upload-artifact@v4
        with:
          name: results
          path: results.json

      - name: Read results for email
        id: results
        run: |
          RESULTS=$(cat results.json)
          TOTAL=$(echo $RESULTS | python3 -c "import sys,json; d=json.load(sys.stdin); print(d['total'])")
          PASSED=$(echo $RESULTS | python3 -c "import sys,json; d=json.load(sys.stdin); print(d['overall_passed'])")
          FAILED=$(echo $RESULTS | python3 -c "import sys,json; d=json.load(sys.stdin); print(d['total'] - d['overall_passed'])")
          OVERALL=$(echo $RESULTS | python3 -c "import sys,json; d=json.load(sys.stdin); print(d['overall_accuracy'])")
          ROUTING=$(echo $RESULTS | python3 -c "import sys,json; d=json.load(sys.stdin); print(d['routing_accuracy'])")
          COMPLETION=$(echo $RESULTS | python3 -c "import sys,json; d=json.load(sys.stdin); print(d['completion_accuracy'])")
          echo "total=$TOTAL" >> $GITHUB_OUTPUT
          echo "passed=$PASSED" >> $GITHUB_OUTPUT
          echo "failed=$FAILED" >> $GITHUB_OUTPUT
          echo "overall=$OVERALL" >> $GITHUB_OUTPUT
          echo "routing=$ROUTING" >> $GITHUB_OUTPUT
          echo "completion=$COMPLETION" >> $GITHUB_OUTPUT

      - name: Send results email
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: smtp.gmail.com
          server_port: 465
          secure: true
          username: ${{ secrets.EMAIL_USERNAME }}
          password: ${{ secrets.EMAIL_APP_PASSWORD }}
          to: ${{ github.event.inputs.recipient_email || secrets.EMAIL_TO }}
          from: OpenCode Evaluator <${{ secrets.EMAIL_USERNAME }}>
          subject: "Evaluation Results — ${{ steps.results.outputs.overall }}% accuracy (${{ steps.results.outputs.passed }}/${{ steps.results.outputs.total }} passed)"
          body: |
            Multi-Agent Evaluation Complete

            Sent to: ${{ github.event.inputs.recipient_email || secrets.EMAIL_TO }}

            Run at:  ${{ github.run_id }}
            Commit:  ${{ github.sha }}
            Branch:  ${{ github.ref_name }}

            Results:
              Total:               ${{ steps.results.outputs.total }}
              Passed:              ${{ steps.results.outputs.passed }}
              Failed:              ${{ steps.results.outputs.failed }}
              Overall Accuracy:    ${{ steps.results.outputs.overall }}%
              Routing Accuracy:    ${{ steps.results.outputs.routing }}%
              Completion Accuracy: ${{ steps.results.outputs.completion }}%

            Full results attached as results.json artifact.
            View run: https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}
          attachments: results.json